@{
    ViewBag.Title = "撤销发布";
}

<div class="page-header">
    <h1>撤销发布</h1>
</div>
@using (Html.BeginForm())
{
    <p>撤销发布后，求职者将无法在招聘网站上看到此条公告。确实要这样做吗？</p>
    <button type="submit" class="btn btn-danger">撤销发布</button>
    @Html.ActionLink("取消", "Detail", new { id = this.Request["id"] }, new { @class = "btn btn-default" })
}