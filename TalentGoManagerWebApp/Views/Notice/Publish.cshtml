@{
    ViewBag.Title = "发布";
}

<div class="page-header">
    <h1>发布</h1>
</div>
@using (Html.BeginForm())
{

    <p>Publish?</p>
    <button type="submit" class="btn btn-primary">Publish</button>
    @Html.ActionLink("Cance", "Detail", new { id = this.Request["id"] })
}