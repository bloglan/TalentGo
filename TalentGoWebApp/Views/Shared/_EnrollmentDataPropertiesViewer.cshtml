@model Enrollment

@*显示报名资料相关属性*@

<div class="panel panel-default">
	<div class="panel-body">
		@Html.Partial("EnrollmentStatePartial", Model)
		<ul class="list-unstyled">
			<li>创建时间：@Model.WhenCreated</li>
			@if (Model.WhenChanged.HasValue)
			{
				<li>最后修改时间：@Model.WhenChanged.Value</li>
			}

			@if (Model.WhenCommited.HasValue)
			{
				<li>提交时间：@Model.WhenCommited.Value</li>

				if (Model.WhenAudit.HasValue)
				{
					<li>
						审核@(Model.Approved.Value ? "通过" : "未通过")！

						@if (!string.IsNullOrEmpty(Model.AuditMessage))
					{
							@:附加说明： @Model.AuditMessage
			         }
					</li>
					if (Model.WhenAnnounced.HasValue)
					{
						if (Model.IsTakeExam.Value == true)
						{
							<li>已于 @Model.WhenAnnounced.Value 声明确认参加考试。</li>
						}
						else {
							<li>已于 @Model.WhenAnnounced.Value 声明确认放弃考试。</li>
						}
					}
					else {
						if (Model.Approved.Value == true)
						{
							<li>尚未确认是否参加考试。请于 @Model.RecruitmentPlan.AnnounceExpirationDate.Value.ToString("yyyy-MM-dd HH:mm") 前尽快确认。</li>
						}

					}
				}
				else
				{
					<li>报名已提交，正在审核中……审核结果将在不早于报名截止时间之后告知。</li>
				}

			}
			else
			{
				<li>尚未提交。请及时填写并务必在报名截止时间 @Model.RecruitmentPlan.EnrollExpirationDate.Value.ToString("yyyy-MM-dd HH:mm") 之前提交。</li>
			}



		</ul>

	</div>
</div>
